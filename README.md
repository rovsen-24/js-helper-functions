## helper functions for your javascript code
- you can run files by `node 'filename'(CLI commands if exist)` or by console in browser
- `process.argv` is command line argument
 'unit testing will be written soon' 
